import 'package:flutter/material.dart';
import 'app_bar.dart';

class UsedInventory extends StatelessWidget {
  const UsedInventory({Key? key}) : super(key: key);

  @override 
  Widget build(BuildContext context) {
    return const Scaffold(
      
    );
  }
}